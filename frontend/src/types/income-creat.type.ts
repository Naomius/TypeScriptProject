export type IncomeCreatType = {
    error: boolean,
    message: string,
    category: {
        id: number,
        title: string,
        user_id: number
    }
}

export type ExpanseCreatType = {
    error: boolean,
    message: string,
    category: {
        id: number,
        title: string,
        user_id: number
    }
}